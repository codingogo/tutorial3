<p id="notice"><%= notice %></p>
<div class="jumbotron white center shadow-text"> 
  <h1>Welcome!</h1> 
  <h2>엣치 마켓에 오신것을 환영합니다.</h2> 
</div>

<h4>상품 </h4>

  <div class="center">
    <div class="row">
      <% @listings.each do |listing| %>
        <div class="col-md-3">
          <div class="thumbnail">
            <%= link_to image_tag(listing.image_url), listing %>
          </div> 
          <div class="caption"> 
            <h3><%= listing.name %></h3>
            <p><%= listing.description %></p>
            <p><%= number_to_currency(listing.price) %></p>
            <p><%= "판매자: #{listing.user.name}" %></p>
          </div>  
        </div>

      <% end %>
    </div>
  </div>  

<br>

